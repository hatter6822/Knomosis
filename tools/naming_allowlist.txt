# Canon naming-audit allowlist.
#
# Format: one entry per line, `path:matched-token`.
# Empty lines and lines starting with `#` are ignored.
#
# This file should be EMPTY in steady state.  Entries are
# documented exceptions for content words that the audit's
# substring check might mis-classify in narrow circumstances.
# When adding an entry: explain in a comment why the content
# word is legitimate and not a process / provenance token.
